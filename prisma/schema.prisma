generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// CHANGE_ME: add your models below
// Tip: start with one model, then run `npx prisma migrate dev --name init`

model Example {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
