import express from "express";

const router = express.Router({
  caseSensitive: true,
  strict: true,
});

// CHANGE_ME: mount your domain routers here, e.g. router.use("/tasks", tasksRoutes)

export default router;
